class Solution {
    static boolean searchInSorted(int arr[], int k) {
       int n=arr.length;
       boolean flag=false;
       for(int i=0;i<n;i++){
           if(arr[i]==k){
               flag=true;
               
           }
       }
       return flag;
    }
}
